apiVersion: v1
kind: Service

metadata:
  name: flask-api
  namespace: flask-api

spec:
  selector:
    app: flask-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: ClusterIP